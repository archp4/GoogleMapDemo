<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MiniMap">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="500dp"
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/miniMapHolder"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/humber_downtown"
        android:id="@+id/btn_downtown"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/miniMapHolder"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/humber_north"
        android:id="@+id/btn_north"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/btn_downtown"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/humber_lakeshore"
        android:id="@+id/btn_lakeshore"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/btn_north"
        />

</androidx.constraintlayout.widget.ConstraintLayout>